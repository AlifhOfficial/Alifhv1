/**
 * Legacy entrypoint for comprehensive listing seeding.
 *
 * This project previously had multiple listing seed scripts that diverged from
 * the current `car_listing` schema. Keep this filename for convenience, but
 * delegate to the schema-aligned seeder.
 */

import './seed-listings-new';

