{
  "name": "@alifh/preprocessing",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bun --watch --env-file=../../.env.local src/index.ts",
    "build": "bun build ./src/index.ts --outdir dist --target bun --packages external",
    "start": "bun run dist/index.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.750.0",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
