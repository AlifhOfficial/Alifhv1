# Railway configuration for WebSocket server
# Must be deployed as a separate service in the same project

[build]
builder = "railpack"
buildCommand = "cd apps/ws && bun install && bun run build"

[deploy]
startCommand = "cd apps/ws && bun run dist/index.js"
