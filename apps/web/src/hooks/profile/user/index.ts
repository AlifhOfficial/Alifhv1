/**
 * Profile Hooks - Unified with React Query
 * Clean state management following UI → Hook → API → Query → DB flow
 */

export { useUserProfile, type UserProfile, type UserProfileUpdate } from './use-user-profile';
