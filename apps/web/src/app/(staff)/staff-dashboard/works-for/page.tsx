/**
 * Staff Works For Page
 * Shows the partner profile that the staff member works for
 */

'use client';

import { StaffWorksFor } from '@/components/staff/staff-works-for';

export default function StaffWorksForPage() {
  return <StaffWorksFor />;
}