# Bun Configuration for Alifh Platform

[install]
# Save exact versions for reproducible builds
exact = true

# Disable global disk cache
cache = false

# Prefer npm-style hoisting to avoid duplicate native packages in Expo monorepos
linker = "hoisted"
